Ventilation:
  module: ventilation
  class: Ventilation
  dependencies:
    - Common
  config:
    climate: climate.z_wave_plus_thermostat
    on_low_interval: 900 # 15m
    on_auto_interval: 9900 # 2h 45m
    sleep_input: input_boolean.sleep
  constrain_enabled:
